# Configuration file for fw_(printenv|setenv) (libubootenv).
# u-boot env lives in a file on the FAT partition labelled 'env',
# which fstab mounts at /media/env. Size must match CONFIG_ENV_SIZE.
#
# Device                Offset      Env. size
/media/env/uboot.env    0x0000      0x2000
