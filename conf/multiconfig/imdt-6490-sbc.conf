# Multiconfig for the 6490 SBC, used by the combined kas configs so both
# boards build in one bitbake invocation. Each multiconfig needs its own
# TMPDIR; the sstate and download caches stay shared.
MACHINE = "imdt-6490-sbc"
TMPDIR = "${TOPDIR}/tmp-imdt-6490-sbc"
